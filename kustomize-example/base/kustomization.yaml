commonAnnotations:
  note: base-env
resources:
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - literals:
    - FOO=Bar
    name: example-configmap-1
secretGenerator:
  - name: example-secret-1
    envs:
      - secret.env  # Create from secret.env.example; do not commit real secrets
generatorOptions:
  annotations:
    note: generate
  disableNameSuffixHash: true
  labels:
    type: generated
