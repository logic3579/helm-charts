commonAnnotations:
  note: base-env
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - literals:
    - FOO=Bar
    name: example-configmap-1
secretGenerator:
  - literals:
    - username=admin
    - password=changeme  # Replace with actual secret; do not commit real credentials
    name: example-secret-1
generatorOptions:
  annotations:
    note: generate
  disableNameSuffixHash: true
  labels:
    type: generated
