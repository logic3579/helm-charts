{{- if .Values.nginxConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend-app.fullname" . }}-nginx
  labels:
    {{- include "frontend-app.labels" . | nindent 4 }}
data:
  default.conf: |
    {{- .Values.nginxConfig.customConf | nindent 4 }}
{{- end }}
